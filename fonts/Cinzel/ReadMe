Cinzel Variable Font
====================

This download contains Cinzel as both a variable font and static fonts.

Cinzel is a variable font with this axis:
  wght

This means all the styles are contained in a single file:
  Cinzel/Cinzel-VariableFont_wght.ttf

If your app fully supports variable fonts, you can now pick intermediate styles
that aren’t available as static fonts. Not all apps support variable fonts, and
in those cases you can use the static font files for Cinzel:
  Cinzel/static/Cinzel-Regular.ttf
  Cinzel/static/Cinzel-Medium.ttf
  Cinzel/static/Cinzel-SemiBold.ttf
  Cinzel/static/Cinzel-Bold.ttf
  Cinzel/static/Cinzel-ExtraBold.ttf
  Cinzel/static/Cinzel-Black.ttf
